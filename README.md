# password-strength
This project is a Python-based security tool that validates the strength of user passwords by checking for length, numbers, and special characters to ensure they meet security standards.
